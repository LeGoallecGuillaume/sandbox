import { WaitingNotePipe } from './waiting-note.pipe';

describe('WaitingNotePipe', () => {
  it('create an instance', () => {
    const pipe = new WaitingNotePipe();
    expect(pipe).toBeTruthy();
  });
});
